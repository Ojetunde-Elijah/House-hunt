# Optional: set when frontend is deployed (e.g. on Vercel) so it talks to your backend.
# Leave unset for local dev (Vite proxy will use backend on localhost).
VITE_API_BASE_URL=https://house-hunt-urr1.onrender.com
